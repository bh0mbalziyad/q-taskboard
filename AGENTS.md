## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues on `bh0mbalziyad/q-taskboard` (the `ziyad` remote) via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Only two labels: `ready-for-agent` and `done`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
